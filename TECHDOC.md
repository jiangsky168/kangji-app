# 康迹技术方案

> 版本：1.0  
> 编制日期：2026-08-26  
> 适用范围：面向中国大陆用户的代谢健康档案 App  
> 价格口径：人民币含税与否以厂商下单页为准。公开价格查证于 2026-08-26。动态价格、商务报价和无法从公开页面确认的价格均标记为“待核实”。

## 0. 决策摘要

### 0.1 一句话方案

以 Flutter 作为患者主端，FastAPI 作为模块化后端，PostgreSQL 作为主数据库，业务与图片部署在阿里云中国大陆地域；检验报告先接百度医疗检验报告单 OCR，文本 AI 采用阿里云百炼华北 2 地域的千问 Flash，食物识别采用百度菜品识别加千问 VL 低置信度兜底，所有 AI 只接收最小化后的结构化数据。

### 0.2 关键决策

| 决策项 | 推荐 | 原因 |
|---|---|---|
| 患者端 | Flutter 主端，微信登录和分享；小程序后置为轻入口 | 一套客户端代码，同时保留相机、蓝牙厂商 SDK、后台任务、推送和订阅能力 |
| 后端 | Python 3.12、FastAPI、SQLAlchemy 2、Alembic | 小团队只维护一种后端语言，OCR、数据处理和 AI 评测工具链完整，性能可横向扩展 |
| 主库 | PostgreSQL 16 托管版 | 事务、JSONB、约束、部分索引和分析查询适合纵向健康档案；不另建时序库 |
| 图片 | 中国大陆 OSS 私有桶，KMS 加密，短时签名 URL | 原图不进入数据库，不公开读，不通过日志泄露 URL |
| OCR | 百度医疗检验报告单 OCR 起步，500 至 1000 张脱敏样本竞测后签约；规模期采用 PaddleOCR 主识别加百度低置信度兜底 | 百度现成返回项目名、结果、单位、参考区间和异常提示；混合方案控制规模成本 |
| 文本 AI | 千问 Flash，中国大陆地域；DeepSeek V4 Flash 作为经合同确认后的备选 | 千问有明确华北 2 地域和公开价格，便于满足数据不出境和供应商治理 |
| 食物识别 | 百度菜品识别优先，千问 VL 仅处理复合餐和低置信度图片 | 专用接口便宜且返回菜名、卡路里和置信度，VL 负责长尾理解，不直接给出诊疗建议 |
| 部署 | MVP 为一台应用机加托管数据库和 OSS，全部容器化；成长阶段改为双应用节点和独立工作节点 | 起步简单，数据库和对象存储仍有托管备份；业务增长后可平滑扩容 |
| 合规 | 按等保三级目标设计，尽早开展定级咨询；上线前完成 ICP备案和 App 备案、个人信息保护影响评估、供应商协议和 AI 标识 | 医疗健康信息属于敏感个人信息，不能把合规留到开发结束 |

### 0.3 明确不做

- MVP 不做在线问诊、诊断、处方、医生随访和医院 HIS 对接。
- 不将化验单原图直接发送给文本 LLM。
- 不用海外分析、崩溃监控、推送或大模型服务处理患者数据。
- 不在 MVP 引入 Kubernetes、Elasticsearch、独立时序数据库和微服务拆分。
- 不承诺支持所有蓝牙设备，只支持经过测试的型号白名单。

## 1. 目标、边界与验收标准

### 1.1 技术目标

1. 用户完成拍照后，系统在 30 秒内给出可编辑的结构化检验结果。
2. 所有 OCR 结果必须经过用户确认后才进入正式趋势和预警计算。
3. AI 输出不参与数值判定。异常标记、单位换算和阈值匹配由版本化规则引擎完成。
4. 所有患者数据、图片、日志中的敏感字段、OCR 和 AI 处理均位于中国大陆。
5. 低端安卓测试机上首屏冷启动目标小于 2.5 秒，列表滚动维持 50 FPS 以上，安装包目标小于 50 MB。具体设备基线在立项周确认。

### 1.2 上线验收门槛

| 类别 | 验收门槛 |
|---|---|
| OCR | 500 至 1000 张脱敏报告盲测，核心指标名称、数值、单位、参考区间的字段级 F1 不低于 95%；自动接受字段的数值准确率不低于 99.5% |
| OCR 降级 | 低置信度、单位冲突、参考区间缺失或数值越界时必须进入人工确认，不得自动入库 |
| AI | 200 组金标准案例评测，禁止出现诊断结论、擅自改药、确定性病因判断；结构化 JSON 成功率不低于 99% |
| 蓝牙 | 首批每个支持型号至少完成 20 台次配对、断连重连、重复数据和时间漂移测试 |
| 性能 | API 读请求 p95 小于 400 ms，写请求 p95 小于 800 ms，OCR 和 AI 使用异步任务，不阻塞主请求 |
| 安全 | 无公开 OSS 对象、无明文敏感字段日志、管理端强制 MFA、关键操作审计可追溯 |
| 恢复 | 数据库恢复点目标 RPO 不超过 15 分钟，恢复时间目标 RTO 不超过 4 小时；MVP 演练一次，正式上线后每半年演练 |

## 2. 端侧技术选型

### 2.1 方案比较

评分为 1 至 5，5 表示更适合本项目。评分用于决策，不代表通用技术优劣。

| 方案 | 中老年触达 | 蓝牙和厂商 SDK | 相机与上传 | 低端机性能 | 3 至 5 人维护成本 | 医生端扩展 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---|
| Swift 加 Kotlin 原生双端 | 3 | 5 | 5 | 5 | 1 | 2 | 体验最好，但两套代码和测试矩阵超出小团队经济性 |
| Flutter | 3 | 4 | 5 | 4 | 4 | 3 | 推荐作为患者主端，厂商 SDK 通过平台通道封装 |
| React Native | 3 | 3 | 4 | 3 | 3 | 3 | JS 团队可选，但本项目蓝牙桥接和低端安卓稳定性风险高于 Flutter |
| 微信小程序优先 | 5 | 2 | 4 | 4 | 4 | 2 | 适合获客和轻量归档，不适合承担全部蓝牙、后台提醒和系统健康数据互通 |
| uni-app | 5 | 2 | 4 | 3 | 4 | 2 | 快速覆盖多端，但原生医疗设备 SDK 和复杂后台任务仍需大量条件编译与插件维护 |

### 2.2 推荐方案

采用“Flutter 主端，加微信生态入口”的单主端策略：

- Flutter 完成健康档案、报告拍照、蓝牙、趋势、用药提醒、会员和家庭档案。
- 使用微信授权登录、系统分享和微信客服降低安装与使用门槛。
- MVP 不同时维护完整小程序。验证留存后，再做只含登录、拍报告、看摘要和家庭邀请的小程序轻入口。
- 若商务要求小程序先发布，应把蓝牙降级为手动录入和 1 至 2 个标准 GATT 设备试点，不能把厂商原生 SDK 支持写入小程序承诺。

### 2.3 客户端实施要点

- 状态管理保持单一方案，例如 Riverpod；不同时引入多套状态框架。
- 网络层支持幂等键、断点续传、指数退避和离线草稿。
- 拍照后在端侧做裁边、透视矫正、压缩和 EXIF 清理。上传图建议长边不超过 2400 px，质量参数按样本压测确定。
- 所有按钮触控区不小于 44 dp，正文默认不小于 16 sp，支持系统字体放大。
- 敏感内容不进入系统剪贴板、通知正文和崩溃日志。
- 蓝牙建立统一 `DeviceAdapter` 接口，厂商差异留在原生插件中；数据同时保存原始值、标准值、设备型号、固件版本和测量时间。

### 2.4 医生端和管理端扩展影响

后期医生端或运营管理端使用 React Web 或 Vue Web，复用同一套 API 和权限系统，不复用 Flutter 页面代码。新增以下模型：

- `organizations`：医院、团队或合作机构。
- `staff_users`：医生、客服、审核员和运营人员。
- `care_relationships`：患者与工作人员的授权关系。
- `consent_grants`：授权范围、目的、期限、撤回时间。
- `audit_logs`：查看、导出、修改、AI 调用和授权操作审计。

如果医生端提供诊断、复诊、处方或治疗方案，产品性质将从健康档案工具转为互联网诊疗相关服务，必须先完成医疗机构合作与资质评估，不能只靠技术扩展上线。

## 3. OCR 方案

### 3.1 供应商比较

公开价格均为厂商公开目录价或资源包价，不含商务折扣。准确率一栏只记录公开声明，不能替代康迹自有样本盲测。

| 方案 | 检验单结构化能力 | 手写体 | 自定义字段 | 公开价格 | 准确率信息 | 适用判断 |
|---|---|---|---|---|---|---|
| 百度医疗检验报告单 OCR | 直接返回项目名、代号、结果、单位、参考区间、结果提示等 | 未声明专项支持，待实测 | 可在业务层映射，复杂模板可另用 iOCR | 后付费 0.8 元/次；1000 次 600 元；1 万次 5500 元；10 万次 45000 元；100 万次 300000 元 | 官方称针对倾斜、弯曲优化，平均单图小于 2 秒，未公布检验单统一准确率 | MVP 首选 |
| 阿里云 OCR | 通用表格、全文高精度和文档结构化；公开医疗专项主要是核酸报告，不是通用检验报告 | 有通用手写体接口，表格接口也有手写参数 | 支持表格信息抽取和字段标注 | 表格按量首档 0.0825 元/次，高精度和手写首档 0.225 元/次；1 万次表格包 550 元 | 无适用于检验单的公开统一数值 | 低价候选，需要自建字段映射 |
| 腾讯云 OCR | 表格 V3 返回单元格并支持 Excel，需自建检验字段映射 | 通用手写体接口 | 无医疗字段模板，业务层可映射 | 表格后付费首档 0.15 元/次；1 万次资源包 800 元；10 万次 5000 元 | 腾讯文档识别页面称表格文字识别准确率 92% 以上，非检验单专项数据 | 通用表格候选 |
| 合合信息 TextIn | 医疗票据接口可分类并返回 `medical_laboratory_report`，医疗解决方案支持检验报告和结构化抽取 | 产品页面支持复杂文档，检验单手写表现待实测 | 支持智能抽取和私有化 | 待核实，需商务报价 | 无公开统一数值 | 必须进入 POC，可能是百度的强备选 |
| PaddleOCR 自建 | PP-StructureV3 支持版面和表格，PP-ChatOCR 支持关键信息抽取；需要自建医疗字典和关系映射 | 有通用能力，医疗手写仍需数据打磨 | 可训练、二次开发和私有部署 | 软件 Apache 2.0，无 API 调用费；服务器、标注、训练和运维单价待压测核实 | 官方提供通用基准，不代表康迹检验单表现 | 规模期降本主方案，不作为无样本 MVP 唯一方案 |
| 讯飞 OCR | 通用印刷体和手写体识别，需自行恢复表格与字段关系 | 支持中英文手写 | 业务层映射 | 1 万次 350 元，10 万次 3200 元，100 万次 30000 元 | 无检验单专项公开数值 | 低价文字层候选，不优先 |

### 3.2 推荐与采购门槛

MVP 推荐百度医疗检验报告单 OCR，但签约必须满足以下条件：

1. 明确服务处理地点在中国大陆。
2. 合同写明图片和识别结果不用于模型训练，不向未披露的第三方提供。
3. 明确保留期限、删除机制、分包商、事故通知和审计配合义务。
4. API 使用图片二进制或 Base64，不传公开 OSS URL，不传姓名以外的业务附加字段。
5. 先用百度、TextIn、阿里表格、腾讯表格和 PaddleOCR 对同一批样本盲测，再以准确率、成本和合同条款共同决策。

若任一供应商不能书面确认境内处理和不训练，立即切换为阿里云中国大陆地域自建 PaddleOCR，供应商能力再好也不例外。

### 3.3 OCR 数据流程

```text
拍照或选图
  -> 端侧裁边、矫正、压缩、去 EXIF
  -> 后端签发短时上传凭证
  -> 上传到中国大陆 OSS 私有桶
  -> 创建 lab_report 和 processing_job
  -> OCR 工作进程读取私有对象并调用境内 OCR
  -> 原始结果加密保存，标准化为 lab_items
  -> 字典映射、单位换算、数值校验、重复项检测
  -> 用户确认或修改
  -> 写入正式指标、触发版本化规则
  -> 生成趋势和可选 AI 解读
```

### 3.4 字段映射与质量控制

- 建立 `lab_item_dictionary`，包含标准码、别名、拼音、英文名、常用单位、合法数值范围和换算规则。
- 原始值与标准值并存，禁止覆盖 OCR 原文。
- 以 `mapping_version` 和 `rule_version` 固化每次处理依据，后续规则升级不得静默改写历史结果。
- 自动接受条件至少包括：OCR 置信度达标、名称唯一映射、单位合法、数值在物理可行范围、参考区间可解析。
- 置信度阈值由盲测结果确定，不直接套用供应商分数。
- 手写报告默认进入全量人工确认。MVP 可只归档图片，不自动生成趋势。

### 3.5 OCR 月成本测算

假设每个 MAU 每月 0.5 份报告，每份平均 1.2 页，即每个 MAU 每月 0.6 次 OCR。

| 阶段 | MAU | OCR 次数/月 | 单价假设 | OCR 成本/月 |
|---|---:|---:|---:|---:|
| MVP | 3000 | 1800 | 0.55 元，按 1 万次包折算 | 990 元 |
| 成长 | 30000 | 18000 | 0.50 元，按 5 万次包折算 | 9000 元 |
| 规模化 | 150000 | 90000 | 0.45 元，按 10 万次包折算 | 40500 元 |

注意：资源包通常需要预付且有一年有效期。上表是成本归集值，不是现金付款节奏。多页 PDF、失败重试、重复上传和供应商计费错误码可能增加实际费用，正式预算增加 10% 安全余量。

## 4. AI 与 LLM 方案

### 4.1 模型现状与比较

| 方案 | 当前可用形态 | 大陆地域确定性 | 公开价格 | 结论 |
|---|---|---|---|---|
| DeepSeek | 官方当前型号为 `deepseek-v4-flash` 和 `deepseek-v4-pro`；`deepseek-chat` 已于 2026-07-24 停用 | 官方统一端点未提供项目级地域选择，需合同确认 | V4 Flash 缓存未命中输入 1 元/百万 Token，输出 2 元/百万 Token；V4 Pro 为 3 元和 6 元 | 文本备选；不把已停用的 `deepseek-chat` 写入生产配置 |
| DeepSeek 视觉 | DeepSeek 官方价格页没有名为 `deepseek-vl` 的稳定生产 API；腾讯 TokenHub 有 `DeepSeek-V4-Flash-Vision-Exp` | TokenHub 可选广州地域 | TokenHub 空闲时段输入 1.5 元、输出 4.5 元，高峰时段 3 元和 9 元/百万 Token | 实验型号，不作为医疗生产主模型 |
| 通义千问 | 百炼华北 2 提供文本和 Qwen-VL 系列 | 明确可选中国大陆华北 2 | `qwen3.7-flash` 在 32K 内输入 0.2 元、输出 0.8 元；`qwen3-vl-8b-instruct` 输入 0.5 元、输出 2 元/百万 Token | 推荐生产主模型 |
| 智谱 GLM | GLM-4V 系列和更新的 GLM 视觉模型 | 中国企业服务，具体处理地域与留存条款需合同确认 | `GLM-4V-Plus-0111` 公开价 4 元/百万 Token；老 `GLM-4V` 为 50 元/百万 Token | 作为评测备选，采购价与地域待合同核实 |
| 豆包视觉 | 火山方舟提供 Doubao Vision 系列，旧型号存在持续迁移 | 中国大陆服务，但具体模型、地域和留存需在控制台与合同确认 | 公开价格页为动态页面，当前目标型号单价待核实 | 能力候选，不作为首期主供应商 |
| 腾讯混元 | TokenHub 广州地域提供 HY Vision | 明确中国大陆广州地域 | `HY-Vision-2.0-Instruct` 输入 7.5 元、输出 17.5 元/百万 Token | 地域清晰但成本较高，可作容灾备选 |

### 4.2 功能分工

| 功能 | 输入 | 执行方式 | 输出约束 |
|---|---|---|---|
| 报告解读 | 用户已确认的结构化指标、历史摘要、规则引擎结论、来源片段 | 千问 Flash | JSON Schema，逐项说明和复查提示，不给诊断 |
| AI 预问诊 | 用户主动输入的症状、用药和近期指标摘要 | 千问 Flash | 只生成就诊准备清单和建议向医生提问的问题 |
| 共病关联分析 | 聚合后的时间序列统计结果 | 统计代码先计算，千问 Flash 只负责转述 | 每条结论带数据范围和不确定性，不写因果结论 |
| 健康周报 | 已计算的达标率、趋势和记录完整度 | 模板优先，LLM 只润色 | 明确数据缺口和规则来源 |
| 食物识别 | 去 EXIF、压缩后的餐食图片 | 百度菜品识别，低置信度再调千问 VL | 返回候选菜名、置信度和可编辑估算，不自动判断摄入量 |

### 4.3 为什么不把报告图片直接交给 LLM

- OCR 专用接口能提供稳定字段结构，成本和回归测试更可控。
- 结构化数据可以先做单位换算、参考区间判断和用户确认。
- LLM 只看到最小数据集，不必接收姓名、医院、条码和报告原图。
- 模型更换时，历史 `lab_items` 不需要重新识别。
- 可将数值判断与语言生成分离，降低幻觉造成错误预警的风险。

### 4.4 食物识别推荐

百度菜品识别公开接口返回菜名、卡路里和置信度，当前后付费首档 0.70 元/千次，即 0.0007 元/次。推荐流程如下：

1. 专用接口先识别主要菜品。
2. 多菜同盘、家常混合菜或低置信度图片再调用千问 VL。
3. 份量由用户选择小份、中份、大份或手动修改，不能从单张照片假定精确克重。
4. 营养和嘌呤数据来自版本化食物成分库，模型只做候选匹配与解释。
5. 所有结果展示“估算值”，不据此调整药物或替代营养师建议。

### 4.5 单次 AI 调用成本

以下按推荐模型目录价和固定 Token 假设计算，图片 Token 以真实 API 返回为准。

| 功能 | Token 假设 | 模型 | 估算成本/次 |
|---|---|---|---:|
| 报告解读 | 输入 2500，输出 900 | qwen3.7-flash | 0.00122 元 |
| AI 预问诊 | 输入 4000，输出 1200 | qwen3.7-flash | 0.00176 元 |
| 关联分析说明 | 输入 6000，输出 1200 | qwen3.7-flash | 0.00216 元 |
| 健康周报 | 输入 4000，输出 800 | qwen3.7-flash | 0.00144 元 |
| 食物 VL 兜底 | 输入 2500，输出 300 | qwen3-vl-8b-instruct | 0.00185 元 |
| 百度菜品识别 | 一张图片 | 百度菜品识别 | 0.0007 元 |

上述成本不包含重试、内容安全、联网检索和供应商优惠。实际生产需按 `usage` 字段记录到 `ai_usage_ledger`，每周比较预算与账单。

### 4.6 AI 安全护栏

- 医学阈值、异常等级、紧急提醒和单位换算全部由临床审核过的规则引擎执行。
- 提示词将 OCR 文本标记为不可信数据，禁止执行报告中可能出现的指令，防止提示注入。
- 输出强制 JSON Schema，解析失败最多重试一次，随后回退到模板解释。
- 只传 `profile_id` 的临时别名和必要指标，不传姓名、手机号、身份证、医院名称、原图 URL。
- 每次保存模型、模型版本、提示词版本、输入字段清单、规则版本、输出和审核状态。
- 页面显著标注“AI 生成，仅供健康管理参考，不构成诊断或治疗建议”。导出内容也保留标识。
- 依据 2025 年生效的 AI 生成合成内容标识规则，为可复制、导出和分享的 AI 文本添加显式标识，并按适用范围评估隐式标识。
- 使用已经备案或登记的大模型服务，并在产品显著位置公示模型名称和备案信息。自建面向公众的生成式服务是否需要安全评估和算法备案，应在上线前由属地网信部门或律师确认。

## 5. 后端架构

### 5.1 技术栈比较

| 技术 | 优点 | 不足 | 判断 |
|---|---|---|---|
| Node.js 和 NestJS | TypeScript 工程化强，模块、依赖注入和 API 开发成熟 | 自建 PaddleOCR、数据评测和科学计算通常还要增加 Python 服务 | 若团队纯前端背景可选 |
| Python 和 FastAPI | OCR、AI、数据清洗和评测工具链最完整，Pydantic 适合严格输入校验 | 需要约束异步和 CPU 密集任务，不能把耗时任务放在 Web 进程 | 推荐 |
| Go | 性能、并发和部署产物优秀 | 医疗字段映射与 AI 生态开发速度较慢，小团队收益不抵额外成本 | 规模期热点服务再评估，不作为起步栈 |

### 5.2 推荐组件

- API：FastAPI、Pydantic v2、OpenAPI。
- ORM 和迁移：SQLAlchemy 2、Alembic。
- 数据库：阿里云 RDS PostgreSQL，中国大陆地域。
- 任务：MVP 使用 PostgreSQL 持久化 `processing_jobs` 表和独立 worker，使用 `FOR UPDATE SKIP LOCKED` 领取任务；任务量超过每分钟 100 个或出现数据库争用后再迁移到托管消息队列。
- 对象存储：OSS 私有桶，不使用公共读。
- 容器：Docker 和阿里云容器镜像服务。
- 监控：云监控、SLS 或同类大陆服务，日志先脱敏再上报。
- API 文档：仅内网或鉴权访问，生产环境关闭交互式调试入口。

### 5.3 逻辑架构

```text
Flutter App
  -> WAF/HTTPS/API
  -> FastAPI
       -> PostgreSQL
       -> OSS 签名上传
       -> processing_jobs
  -> Worker
       -> OCR 适配器
       -> 字段映射与规则引擎
       -> LLM 适配器
       -> 报告与通知生成
  -> 厂商推送/APNs
```

### 5.4 API 设计原则

- 所有写操作接受 `Idempotency-Key`，防止弱网重复提交。
- 每条资源带 `profile_id`，服务端从授权关系校验访问，不信任客户端传入的 `user_id`。
- 上传采用两阶段提交：签名上传，服务端校验对象大小、类型、哈希后确认。
- 列表使用游标分页，避免深分页。
- 删除采用业务软删除加异步物理擦除，完成后保存不可逆的删除凭证，不保留内容。
- API 版本从 `/v1` 起步，新增字段向后兼容，破坏性变更才升大版本。

## 6. 数据库与迁移方案

### 6.1 MySQL 与 PostgreSQL

| 维度 | MySQL | PostgreSQL | 康迹选择 |
|---|---|---|---|
| 团队普及度 | 较高 | 较高 | 都可接受 |
| JSON 数据 | 可用 | JSONB、索引与表达式更完整 | PostgreSQL 优 |
| 数据约束 | 基础能力完整 | Check、部分索引、排除约束更灵活 | PostgreSQL 优 |
| 纵向趋势和统计 | 可完成 | 窗口函数和分析表达能力较强 | PostgreSQL 优 |
| 托管产品 | 阿里 RDS、腾讯 TDSQL 等成熟 | 阿里 RDS PostgreSQL 等成熟 | 选择与主云同地域的 RDS PostgreSQL |

不因规模预期提前分库分表。先按 `profile_id, measured_at` 建组合索引，达到单表数亿行、写入热点或维护窗口无法接受时，再评估原生分区。

### 6.2 核心表迁移

| 现有表 | 迁移建议 |
|---|---|
| `users` | 只保存账号主体。手机号使用 AES-256-GCM 加密，并保存 HMAC 检索值；微信标识加密保存；增加状态、注销时间和隐私协议版本 |
| `health_profiles` | 从 `user_id` 直属关系升级为独立健康主体 `profiles`；通过 `profile_memberships` 支持本人、子女代管和授权撤回 |
| `lab_reports` | `image_url` 改为 `object_key`；增加文件哈希、大小、页数、OCR 供应商、模型版本、状态、确认人、确认时间和删除时间 |
| `lab_items` | 原始名称、原始值、原始单位与标准码、标准值、标准单位并存；增加 OCR 置信度、映射版本、规则版本和用户确认状态 |
| `metric_records` | 数值用 `numeric`，不得用二进制浮点；增加来源设备、原始载荷哈希、时区、去重键和录入者 |
| `diet_records` | 图片对象键与餐次、备注分开；识别结果放入 `diet_analyses`，保存候选、置信度、份量选择和模型版本 |
| `medications` | 药品主记录与 `medication_schedules`、`medication_intakes` 分开，支持调整剂量而不改写历史打卡 |
| `weekly_reports` | 保存统计快照、模板版本、模型版本、来源时间范围和生成状态；不要只保存无法追溯的自由文本 |
| `alert_records` | 保存触发规则、规则版本、阈值来源、确认状态和关闭原因；AI 不直接写预警 |

### 6.3 必须新增的治理表

- `profile_memberships`：家庭成员角色、授权范围、有效期和撤回状态。
- `consent_records`：隐私政策、敏感信息、OCR、AI、家庭共享和二次使用的独立授权记录。
- `processing_jobs`：OCR、AI、导出、删除和周报任务，包含重试次数与死信状态。
- `audit_logs`：主体、动作、资源、结果、IP 摘要、设备摘要和时间，不记录报告内容。
- `data_export_jobs` 与 `deletion_requests`：支持用户权利闭环。
- `ai_usage_ledger`：供应商、模型、输入输出 Token、费用、请求目的和匿名关联号。
- `lab_item_dictionary` 与 `clinical_rules`：标准码、别名、单位、参考规则、来源和审核人。

### 6.4 数据生命周期

| 数据 | 默认策略 |
|---|---|
| 原始检验报告和病历图片 | 用户可见、可导出、可删除；注销后进入删除队列，法律另有保存义务时隔离并告知 |
| OCR 中间图和供应商临时文件 | 识别完成后立即删除，最长保留时间写入供应商合同 |
| 已确认结构化指标 | 随健康档案保留，支持导出和删除 |
| AI 提示词与输出 | 去直接标识后按最短调试期保留，生产默认 30 天；审计元数据可更久，期限由影响评估确认 |
| 安全审计日志 | 建议不少于 6 个月，等保和主管部门要求更长时从其要求 |
| 数据备份 | 与主数据相同权限和删除策略；备份过期后自动清除，恢复时重新执行删除队列 |

## 7. 图片、推送与健康数据互通

### 7.1 图片存储

- OSS Bucket 与数据库同在中国大陆，关闭公共 ACL 和跨境复制。
- 使用服务端 KMS 加密；手机号等高风险字段再使用应用层信封加密。
- 签名 PUT 和 GET 默认 5 分钟有效，权限绑定具体对象键、大小和 MIME 类型。
- 对象键使用随机 ID，不出现手机号、姓名、医院或日期。
- 报告原图不走公共 CDN。食物缩略图如使用 CDN，也必须鉴权且不缓存敏感响应。
- 下载和导出设置水印或用途提示，记录审计事件。

### 7.2 消息推送

- Android 优先接华为、小米、OPPO、vivo 等厂商通道；iOS 使用 APNs。
- 小团队可先用极光或个推聚合厂商通道，企业版价格均为商务报价，标记待核实。
- 推送正文不出现“血糖异常”“尿酸升高”等健康信息，只写“您有一条新的健康提醒，请打开康迹查看”。
- 用药提醒优先本地通知，服务端推送负责跨设备更新和失效补偿。
- 第三方推送 SDK 上线前完成 SDK 个人信息清单、隐私政策披露、延迟初始化和同意前不采集测试。

### 7.3 健康数据互通

- Apple HealthKit、安卓厂商健康平台和设备 SDK 均按最小权限逐项申请。
- 读取与写入权限分开告知，不因用户拒绝某一权限阻断核心手动录入。
- 第三方数据保存 `source`, `source_record_id`, `observed_at`, `received_at`，并按来源键去重。
- 微信运动只适合步数等低敏数据，不作为血糖、血压等医疗指标通道。

## 8. 部署与扩展路径

### 8.1 MVP，0 至 1 万注册用户

- 一台中国大陆 ECS 或轻量服务器运行 API 与独立 worker 容器。
- RDS PostgreSQL 使用托管备份，不与应用同机。
- OSS 私有桶保存图片。
- Nginx 或云负载入口终止 TLS，限制上传体积和速率。
- 每日自动备份，跨可用区能力以所购 RDS 规格为准。
- 不上 ACK，不上 Elasticsearch，不做读写分离。

单机仅指应用计算节点。数据库和图片不能放在同一块本地磁盘，否则单机故障会同时丢失业务与数据。

### 8.2 成长，10 万注册用户

- 两个无状态 API 节点加负载均衡，至少一个独立 worker 节点。
- RDS 升级到高可用规格，增加连接池代理与只读报表任务限流。
- 接入 WAF、集中日志、告警和应用性能监控。
- OCR 与 AI 通过适配器支持供应商切换和流量分配。
- 只有在连接热点、限流或队列争用有证据时引入 Redis 或托管消息队列。

### 8.3 规模化，50 万注册用户

- API 和 worker 分别弹性扩缩，按上传、OCR、AI、周报拆分任务队列。
- 评估 ACK，但只有团队具备容器编排运维能力时迁移。
- `metric_records` 和审计表按时间或 `profile_id` 分区，先压测再实施。
- 自建 PaddleOCR 处理高置信度标准报告，专用 OCR 处理低置信度和复杂版式。
- 多可用区数据库、定期容灾演练、密钥轮换和安全运营成为固定工作。

### 8.4 缓存与搜索

- MVP 不需要 Redis 缓存。首页聚合可以使用 PostgreSQL 索引和短生命周期物化结果。
- MVP 不需要 Elasticsearch。医院、报告和患教内容先用 PostgreSQL 全文检索或前缀检索。
- 当数据库 p95 因重复读明显超标时再加缓存；当搜索需要中文分词、同义词和复杂排序且 PostgreSQL 无法满足时再加搜索服务。

### 8.5 原型阶段部署现状（2026-08-26）

高保真原型（单文件 HTML）已上线，用于手机真机体验和团队评审，**非生产环境**：

| 项 | 内容 |
|---|---|
| 公网地址 | https://jiangsky168.github.io/kangji-app/ |
| 托管方式 | GitHub Pages（public 仓库 jiangsky168/kangji-app，main 分支根目录，legacy 构建） |
| 局域网地址 | http://192.168.1.56:8000/（本机 python http.server，Mac 与手机同 WiFi 时可用） |
| 入口文件 | 仓库根目录 index.html（慢病管理App原型.html 的副本，部署用） |
| 更新方式 | 修改后 `git push origin main`，Pages 约 1 分钟自动重建 |
| 注意事项 | 原型含演示数据；GitHub 访问需代理（HTTPS_PROXY=http://127.0.0.1:7897） |

部署踩坑记录（供以后复用）：
1. gh 交互登录在非交互环境不可用，改用 GitHub OAuth 设备流 API（client_id 用 gh CLI 公开 ID，curl 带代理），用户浏览器输 code 授权后轮询换 token
2. `gh auth login --with-token` 不会配置 git credential helper，需 `git config --global credential.helper "!gh auth git-credential"`
3. 推送前必须确认 index.html 已 git add 并提交，否则 Pages 根路径 404

## 9. 安全与合规清单

### 9.1 法规结论

- 医疗健康信息属于敏感个人信息，只能在特定目的、充分必要和严格保护条件下处理，并取得单独同意。
- 国家卫健委试行办法要求健康医疗大数据存储在境内安全可信的服务器。康迹采取更严格红线，不做任何患者数据出境。
- 委托云、OCR、AI 和推送供应商处理数据时，康迹仍承担处理者责任，合同和技术控制不能缺一。
- App、网站和小程序在境内提供互联网信息服务，需要按适用范围完成 ICP备案与 App 备案。
- 当前产品不提供诊断、处方和互联网问诊，暂不按互联网医院建设。若功能越界，必须重新评估医疗机构执业许可和互联网诊疗要求。

### 9.2 技术控制

| 控制域 | 必做项 |
|---|---|
| 传输 | TLS 1.2 以上，HSTS，禁用弱套件，内部服务优先 VPC 私网 |
| 存储 | 云盘和 OSS 使用 KMS；手机号、微信标识等应用层 AES-256-GCM；密钥与数据分离 |
| 身份 | 用户短信或微信登录；员工独立账号、MFA、最小权限；禁止共享管理员账号 |
| 授权 | 服务端对象级校验；家庭代管按档案和功能授权，可随时撤回 |
| 日志 | 请求 ID 替代姓名和手机号；图片、OCR 文本、Token、Cookie、签名 URL 不入日志 |
| 审计 | 查看、下载、导出、删除、授权、AI 调用、规则修改和管理员操作全留痕 |
| 安全开发 | 依赖扫描、密钥扫描、SAST、API 越权测试、上传文件检测和发布审批 |
| 备份 | 加密备份、恢复演练、备份访问审计、删除请求传播到备份生命周期 |
| 事件响应 | 数据泄露分级、隔离、证据保全、供应商协同、用户与主管机关通知预案 |

### 9.3 隐私政策与授权

隐私政策至少说明：

1. 收集的字段、目的、处理方式、保存期限和必要性。
2. 医疗健康信息属于敏感个人信息，并单独展示其必要性和对权益的影响。
3. 拍照上传前单独说明图片将用于 OCR、归档和可选 AI 解读。
4. 云、OCR、AI、短信、推送和统计 SDK 的名称、处理目的、信息类型和隐私政策链接。
5. 家庭成员代管的授权、撤回、查看记录和未成年人规则。
6. 查阅、更正、复制、导出、删除和注销的入口与处理时限。
7. 不以拒绝非必要权限为由阻断手动录入等核心功能。
8. 供应商变更、用途变化和二次使用必须重新评估并在必要时重新取得同意。

上线前完成个人信息保护影响评估，至少覆盖敏感信息处理、自动化决策、委托处理、家庭共享、药企合作和大规模处理场景。

### 9.4 等保三级启动路径

健康档案并不自动等于法定三级，最终等级取决于系统受破坏后对公民权益、社会秩序和国家安全的影响。康迹建议按三级目标建设，并在第 1 个月请有资质机构开展定级咨询。

1. 明确定级对象、系统边界、网络拓扑和数据流。
2. 编制定级报告，组织专家评审并取得主管部门意见。
3. 向所在地设区市级以上公安机关备案。二级以上系统投入运行后 30 日内办理备案。
4. 按差距分析完成身份鉴别、访问控制、安全审计、入侵防范、备份恢复、制度和人员整改。
5. 委托有资质测评机构开展三级测评并修复问题。
6. 上线后持续监测，按要求开展复测和监督检查。

等保三级咨询、整改、测评和安全产品总价高度依赖系统边界，当前预算标记为待核实，必须取得至少 3 家正式报价后定标。

### 9.5 药企合作的数据方案

第一阶段只提供聚合统计报告，不提供行级健康数据。推荐控制如下：

- 合作目的、指标口径、时间范围和接收方写入单独协议，必要时重新取得用户单独同意。
- 先去除姓名、手机号、设备标识、精确地址、医院流水号、原图和自由文本。
- 年龄改为区间，地区降到足够粗粒度，日期按月或季度聚合。
- 小样本单元抑制，建议 `k >= 10`；极值、稀有病种组合和交叉筛选做重识别风险检查。
- 仅导出汇总表或在受控数据空间内查询，不允许下载明细。
- 用途绑定、查询限额、结果审计、禁止重识别和转授权，并约定违规责任。
- 假名化数据仍然是个人信息。只有处理后无法识别特定自然人且不能复原，才属于法律定义的匿名化。
- 每个合作项目由隐私、法律、临床和数据负责人共同审批。

## 10. 成本估算

### 10.1 统一假设

- 注册用户与 MAU：MVP 为 1 万和 3000，成长为 10 万和 3 万，规模化为 50 万和 15 万。
- 每 MAU 每月 0.5 份报告，每份 1.2 页，即 0.6 次 OCR。
- 每 MAU 每月 12 张饮食照片，其中 10% 进入 VL 兜底。
- 每 MAU 每月 0.5 次报告解读、0.2 次预问诊、0.3 次关联说明和 1 次周报。
- 短信单价按阿里云公开首档 0.045 元/条，分别假设每月 2000、10000、30000 条。
- OSS 标准本地冗余公开示例价为 0.12 元/GB/月，公网出流量示例为 0.8 元/GB。实际地域和资源包价格待下单核实。
- 服务器、RDS、WAF、KMS、日志和企业推送价格受地域、规格、促销和合同影响，表内属于容量规划预算，采购价待核实。

### 10.2 云资源与 API 月成本

| 成本项 | MVP | 成长 | 规模化 |
|---|---:|---:|---:|
| 计算、数据库、OSS、流量、日志、安全 | 1600 至 4400 元 | 8500 至 18500 元 | 41000 至 85000 元 |
| OCR | 990 元 | 9000 元 | 40500 元 |
| 菜品识别 | 25 元 | 252 元 | 1260 元 |
| 文本 LLM 和 VL 兜底 | 16 元 | 158 元 | 791 元 |
| 短信 | 90 元 | 450 元 | 1350 元 |
| 推送企业版 | 免费基础版或待核实 | 待核实 | 待核实 |
| 合计，不含人力 | 2721 至 5521 元 | 18360 至 28360 元 | 84901 至 128901 元 |

### 10.3 人力

以下是内部预算假设，不是招聘平台报价：

| 团队 | 月基础工资假设 | 加 30% 雇主成本后的月成本 |
|---|---:|---:|
| 3 人：1 后端、1 Flutter、1 产品兼测试 | 5.7 万元 | 7.4 万元 |
| 4 人：增加 1 名客户端或测试 | 7.5 万元 | 9.8 万元 |
| 5 人：2 后端、2 客户端、1 产品测试 | 9.7 万元 | 12.6 万元 |

工资需按实际城市、资历、社保公积金和招聘渠道核实。规模化阶段 5 人团队只能维持核心研发，不足以覆盖 7x24 运维、客服、临床审核和安全运营。

### 10.4 三档总预算

| 阶段 | 技术运行费 | 人力和第三方 | 总预算 |
|---|---:|---:|---:|
| MVP，0 至 6 个月 | 云与 API 约 1.6 至 3.3 万元/6个月 | 3 至 5 人约 44 至 76 万元；测试设备 2 至 5 万元；法律、临床、等保咨询和测评预留 8 至 25 万元，待核实 | 约 56 至 109 万元/6个月 |
| 成长，6 至 12 个月 | 1.84 至 2.84 万元/月 | 5 人约 12.6 万元/月；临床、安全、客服外部支持 1 至 3 万元/月，待核实 | 约 15.4 至 18.4 万元/月 |
| 规模化，50 万用户 | 8.49 至 12.89 万元/月 | 5 人基础研发 12.6 万元/月；运维、客服、临床和安全支持 2 至 5 万元/月，待核实 | 约 23.1 至 30.5 万元/月 |

### 10.5 单用户成本与订阅毛利

| 阶段 | 云与 API 分摊/MAU/月 | 纯边际调用成本/MAU/月 |
|---|---:|---:|
| MVP | 0.91 至 1.84 元 | 约 0.35 至 0.45 元 |
| 成长 | 0.61 至 0.95 元 | 约 0.35 至 0.45 元 |
| 规模化 | 0.57 至 0.86 元 | 约 0.32 至 0.42 元 |

12.8 元/月订阅对单个付费用户有足够的技术边际毛利，但免费用户仍会消耗 OCR 和云资源。不能只用“12.8 减去 0.4”判断整体毛利。

按中国大陆 App Store 自 2026-03-15 起的公开规则，标准佣金为 25%，符合小型企业计划的佣金为 12%。在 12% 佣金下，成长和规模化阶段仅覆盖云与 API 的付费转化率盈亏点约为 5.1% 至 8.4%；在 25% 佣金下约为 5.9% 至 9.9%。这还不含人力、退款、税费、客服和获客。

建议经营门槛：

- 将 MAU 付费转化率目标定为不低于 10%。
- 免费版设置 OCR、AI 解读和 VL 的月度合理额度，超额要求订阅或等待下月恢复。
- 年卡 128 元折合 10.67 元/月，需单独测算续费率与渠道佣金。
- 安卓应用商店、微信和支付宝实际费率待核实，不将未经确认的费率写入预算。
- 每月按供应商账单重算“每确认报告成本”“每 AI 解读成本”和“每 MAU 成本”。

## 11. 开发排期

假设 4 人核心团队：1 名后端兼 AI、1 名后端、1 名 Flutter、1 名产品兼测试。第二名客户端或专职测试可将风险较高阶段并行化。

| 周期 | 里程碑 | 交付物 | 验证 |
|---|---|---|---|
| 第 1 至 2 周 | 架构和 POC | 数据流图、威胁模型、数据库初稿、OCR 样本规范、供应商问卷 | 评审数据不出境链路和 MVP 范围 |
| 第 3 至 5 周 | 基础能力 | Flutter 框架、登录、建档、手动指标、OSS 上传、API、数据库迁移 | 端到端登录与上传测试，低端安卓基线 |
| 第 4 至 8 周 | OCR 竞测与接入 | 500 至 1000 张脱敏样本、供应商盲测、确认页、字段字典、任务重试 | 达到 OCR 验收门槛，生成差错报告 |
| 第 7 至 10 周 | 档案闭环 | 报告列表详情、趋势、预警规则、删除导出、家庭权限基础 | 越权测试、规则版本回归、用户确认闭环 |
| 第 9 至 12 周 | 饮食和提醒 | 饮食拍照、菜品识别、用药计划、本地通知、厂商推送 | 识别可编辑，通知不含敏感正文 |
| 第 11 至 15 周 | AI 功能 | 报告解读、关联说明、预问诊、周报、AI 标识和用量台账 | 金标准安全评测、JSON 成功率、成本核对 |
| 第 12 至 16 周 | 蓝牙试点 | 2 个厂商、每厂商 1 至 2 个型号、断连和重复数据处理 | 实机矩阵验收，不支持型号明确提示 |
| 第 15 至 18 周 | 会员和发布准备 | 订阅、权益校验、隐私中心、App 备案材料、商店材料 | 沙盒支付、恢复购买、注销删除演练 |
| 第 18 至 20 周 | 封闭测试和上线 | 压测、安全测试、备份恢复、监控告警、灰度发布 | 上线清单签字，核心指标看板可用 |

合规工作从第 1 周并行启动：

- 第 1 至 4 周：公司主体、域名、ICP备案和 App 备案准备，个人信息保护影响评估启动。
- 第 2 至 8 周：供应商数据处理协议、SDK 清单、临床规则审核。
- 第 3 至 12 周：等保定级咨询、系统边界和整改清单。
- 第 12 周以后：按当地公安和测评机构流程办理备案与测评。具体周期待属地确认，不承诺与 20 周研发排期完全同步。

## 12. 风险与备选

### 12.1 OCR 准确率不足

风险：医院版式多、拍摄反光、表格无边框、单位粘连和手写内容会造成错列或错值。

对策：

- 端侧质量检测不通过时引导重拍。
- 所有结果进入确认页，数值和单位高亮可改。
- 低置信度走第二 OCR 或人工确认，不生成自动预警。
- 保存原图与字段坐标，用户可对照修改。
- 规模期以 PaddleOCR 处理高置信度标准版式，复杂样本回落专用 OCR。

### 12.2 LLM 医疗越界和幻觉

风险：模型可能把相关性写成因果、给出诊断、擅自建议停药或遗漏紧急情况。

对策：

- 数值判断、红旗症状和紧急提示由临床规则引擎负责。
- AI 只解释已确认数据，输出受 JSON Schema 和禁用语规则约束。
- 每条医学结论绑定经审核的来源版本，无法匹配来源时不生成确定性结论。
- 明示 AI 生成和非诊疗边界，提供一键反馈。
- 定期抽检高风险输出，模型升级必须重跑金标准集。

### 12.3 蓝牙设备兼容性

风险：厂商 SDK 授权、协议私有、安卓后台限制和固件差异会导致连接失败或重复数据。

对策：

- 只承诺白名单型号，优先开放协议或有正式 SDK 支持的厂商。
- 与三诺、鱼跃等厂商签署 SDK、测试设备和版本支持协议，费用待核实。
- 统一设备适配层，原始数据带设备和固件版本。
- 手动录入永远可用，蓝牙失败不阻断健康记录。

### 12.4 App 审核

风险：医疗表述、订阅、隐私权限、后台蓝牙和 SDK 数据采集可能导致拒审。

对策：

- 商店文案使用“健康档案和健康管理参考”，不声称诊断或疗效。
- iOS 数字订阅使用 App 内购买，提供恢复购买、取消说明和明确权益。
- 权限按场景触发，首次启动不一次性索取相机、蓝牙、相册和通知。
- 审核备注提供演示账号、功能边界和硬件演示视频。

### 12.5 备案与资质

| 项目 | 当前判断 | 时点 |
|---|---|---|
| ICP备案 | 中国大陆部署网站和 API 域名必须办理 | 对外服务前 |
| App 备案 | 境内提供 App 互联网信息服务必须办理 | 上架和提供服务前 |
| 公安联网备案 | 按所在地要求办理 | ICP 后按属地时限 |
| 等保定级备案 | 建议按三级目标建设，实际级别经定级评审 | 开发早期启动，投入运行后按规定时限备案 |
| 生成式 AI 备案或登记 | 使用已备案模型并公示；康迹自身是否另行备案取决于服务形态 | 公测前向属地网信部门核实 |
| 互联网医疗机构资质 | 当前不做诊疗，暂不申请 | 一旦增加诊断、处方、复诊或医生服务，必须先评估 |
| 医疗器械软件注册 | 当前定位为档案和健康管理工具，是否构成医疗器械取决于预期用途和宣传 | 产品文案冻结前请法规顾问评估 |

## 13. 上线前决策清单

1. 确认公司主体、主要运营地和备案省份。
2. 确认 Flutter 主端，不同时开发完整小程序。
3. 获取百度和 TextIn 的数据处理协议与正式报价。
4. 完成 500 至 1000 张脱敏报告的多供应商盲测。
5. 确认阿里云中国大陆地域、RDS 规格、OSS KMS 和日志方案。
6. 确认千问模型的地域、备案号、数据保留和不训练条款。
7. 冻结临床规则来源、审核人和更新流程。
8. 确认首批蓝牙厂商、型号、SDK 授权和测试设备。
9. 完成隐私影响评估、隐私政策、单独同意和 SDK 清单。
10. 取得等保机构、法规顾问和临床顾问正式报价，所有待核实价格更新入预算。

## 14. 官方资料与价格来源

以下链接均为本方案查证时使用的官方资料。供应商会调整价格，采购前仍需复核控制台和合同。

### OCR 与图像识别

- [百度医疗检验报告单接口](https://cloud.baidu.com/doc/OCR/s/Ekvakju92)
- [百度医疗票据 OCR 价格](https://cloud.baidu.com/doc/OCR/s/akreo13lc)
- [百度 OCR 总价格页](https://cloud.baidu.com/product-price/ocr.html)
- [阿里云 OCR 按量价格](https://help.aliyun.com/zh/ocr/product-overview/pay-as-you-go)
- [阿里云 OCR 资源包](https://help.aliyun.com/zh/ocr/product-overview/resource-plans)
- [阿里云表格和手写能力](https://help.aliyun.com/zh/ocr/product-overview/common-character-recognition-1)
- [腾讯云 OCR 价格](https://buy.cloud.tencent.com/price/ocr)
- [腾讯云表格识别 V3](https://cloud.tencent.com/document/api/866/86721)
- [TextIn 医疗票据与检验报告接口](https://www.textin.com/document/legacy/medical-bill-recognize)
- [TextIn 医疗文档方案](https://www.textin.com/solutions/medical-document-solution)
- [PaddleOCR PP-StructureV3](https://www.paddleocr.ai/main/en/version3.x/algorithm/PP-StructureV3/PP-StructureV3.html)
- [PaddleOCR 项目与许可证](https://github.com/PaddlePaddle/PaddleOCR)
- [讯飞手写 OCR 与套餐](https://www.xfyun.cn/doc/words/wordRecg/API.html)
- [百度菜品识别接口](https://cloud.baidu.com/doc/IMAGERECOGNITION/s/tk3bcxbb0)
- [百度图像识别价格](https://cloud.baidu.com/product-price/imagerecognition.html)

### 大模型

- [DeepSeek 当前模型与价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing)
- [DeepSeek 更新日志](https://api-docs.deepseek.com/zh-cn/updates/)
- [阿里云百炼模型价格](https://help.aliyun.com/zh/model-studio/model-pricing)
- [智谱模型价格](https://bigmodel.cn/pricing)
- [火山方舟文档](https://www.volcengine.com/docs/82379/1541594)
- [腾讯 TokenHub 价格](https://cloud.tencent.cn/document/product/1823/130055)
- [腾讯 TokenHub 产品页](https://cloud.tencent.cn/product/tokenhub)

### 云资源与渠道

- [阿里云 OSS 计费](https://help.aliyun.com/zh/oss/billing-overview/)
- [阿里云 RDS MySQL 规格价格示例](https://help.aliyun.com/zh/rds/apsaradb-rds-for-mysql/selection-guide)
- [阿里云 ECS 公网带宽计费](https://help.aliyun.com/zh/ecs/public-bandwidth/)
- [阿里云国内短信价格](https://help.aliyun.com/zh/sms/product-overview/billing-of-messages-sent-to-chinese-mainland)
- [Apple 中国大陆 App Store 佣金调整](https://developer.apple.com/cn/news/?id=dadukodv)
- [Apple App 审核指南](https://developer.apple.com/cn/app-store/review/guidelines/)

### 法规与合规

- [中华人民共和国个人信息保护法](https://www.cac.gov.cn/2021-08/20/c_1631050028355286.htm)
- [中华人民共和国数据安全法](https://sdca.miit.gov.cn/zwgk/fgbz/art/2026/art_ce88eeb3d3744672a54923061c125885.html)
- [国家健康医疗大数据标准、安全和服务管理办法](https://www.nhc.gov.cn/mohwsbwstjxxzx/s8553/201809/742308fad8ed47fb85b9675ee4b6c6eb.shtml)
- [信息安全等级保护管理办法](https://www.miit.gov.cn/jgsj/xxjsfzs/xxgk/art/2020/art_a981907528694176a61b5b52c2d19895.html)
- [移动互联网应用程序备案通知](https://www.gov.cn/zhengce/zhengceku/202308/content_6897341.htm)
- [生成式人工智能服务管理暂行办法](https://www.cac.gov.cn/2023-07/13/c_1690898327029107.htm)
- [人工智能生成合成内容标识办法](https://www.cac.gov.cn/2025-03/14/c_1743654684782215.htm)
- [GB 45438-2025 AI 生成合成内容标识方法](https://openstd.samr.gov.cn/bzgk/std/newGbInfo?hcno=F32EA2A561F1886CD8D606513512D547&refer=outter)
- [生成式人工智能服务备案公告](https://www.cac.gov.cn/2024-04/02/c_1713729983803145.htm)
- [互联网诊疗监管细则](https://www.nhc.gov.cn/yzygj/c100068/202203/2072f0e8988249e59d942e1b2a933916.shtml)

## 15. 结论

康迹的技术难点不是搭建普通 CRUD App，而是把报告识别、医疗规则、AI 解释、家庭授权和数据合规拆成可验证的独立环节。最稳妥的落地顺序是先用 Flutter、FastAPI、PostgreSQL 和大陆托管云完成报告归档闭环，用专用医疗 OCR 保证起步质量，再以 PaddleOCR 混合路由降低规模成本。AI 成本不是主要矛盾，OCR 准确率、蓝牙设备兼容、免费用户成本和医疗合规才是决定产品能否持续运营的关键。
